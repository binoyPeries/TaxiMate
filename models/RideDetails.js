export default class RideDetails {
  constructor(id, date, totalFare, distance, waitingTime) {
    this.id = id;
    this.date = date;
    this.totalFare = totalFare;
    this.distance = distance;
    this.waitingTime = waitingTime;
  }
}
